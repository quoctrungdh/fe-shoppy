export enum DATE_TIME_LOCALE {
  enGB = 'DD/MM/YYYY, h:mm a',
  custom = 'YYYY MM DD, hh:mm:ss',
  asia = 'DD-MM-YYYY'
}

export enum SORT_TYPE {
  ASCENDING = 'ascending',
  DESCENDING = 'descending'
}